// script.js

// Functionality for story submission
function submitStory(title, content) {
    // Logic to submit a story
    console.log(`Story submitted: ${title}`);
}

// Functionality for comments
function addComment(storyId, comment) {
    // Logic to add a comment to a story
    console.log(`Comment added to story ${storyId}: ${comment}`);
}

// Functionality for search
function searchStories(query) {
    // Logic to search stories
    console.log(`Searching stories for: ${query}`);
}

// Example usage
submitStory('My First Story', 'This is the content of the first story.');
addComment(1, 'Great story!');
searchStories('first');
